False: Es un operador booleano que representa lo opuesto de True.
None: No hay valor nulo en Python. None es un objeto que representa la ausencia de un valor.
True: Es un operador booleano que representa lo opuesto de False.
and: Es un operador lógico en Python que devuelve True si ambos operandos son verdaderos.
as: Se utiliza en la declaración de importación para asignar un nombre local a un módulo importado.
assert: Se utiliza para depuración y pruebas de código.
break: Se utiliza para terminar un bucle.
class: Se utiliza para definir una clase.
continue: Se utiliza para saltar la iteración actual de un bucle.
def: Se utiliza para definir una función.
del: Se utiliza para eliminar un objeto en Python.
elif: Es una palabra reservada para una declaración condicional.
else: Es una palabra reservada para una declaración condicional.
except: Se utiliza en el manejo de excepciones.
finally: Se utiliza en el manejo de excepciones.
for: Se utiliza para crear un bucle for.
from: Se utiliza en la declaración de importación para importar submódulos específicos.
global: Se utiliza para declarar una variable global.
if: Se utiliza para hacer una declaración condicional.
import: Se utiliza para importar módulos al proyecto.
in: Es un operador de pertenencia que comprueba si un valor existe en una secuencia.
is: Es un operador de identidad que comprueba si dos variables apuntan al mismo objeto.
lambda: Se utiliza para crear funciones anónimas.
nonlocal: Se utiliza para trabajar con variables en el ámbito más cercano.
not: Es un operador lógico en Python que invierte el valor booleano.
or: Es un operador lógico en Python que devuelve True si al menos uno de los operandos es verdadero.
pass: Se utiliza como una declaración de relleno donde no se requiere ninguna acción.
raise: Se utiliza para generar un error.
return: Se utiliza para devolver un valor desde una función.
try: Se utiliza en el manejo de excepciones.
while: Se utiliza para crear un bucle while.
with: Se utiliza para envolver la ejecución de un bloque de código dentro de métodos definidos por un administrador de contexto.
yield: Se utiliza en una función como una declaración de retorno, pero devuelve un generador
